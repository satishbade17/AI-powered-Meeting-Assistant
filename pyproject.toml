[project]
name = "videoagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Satish Bade", email = "satishbade347@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.2.3",
    "torchvision>=0.29.0",
]

[project.scripts]
videoagent = "videoagent:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
